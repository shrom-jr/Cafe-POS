Phase 2 Complete Polish: Full-Height Top-Aligned Cart, Un-truncated Customer Name & Minimalist Menu Cards

Refactor `src/screens/OrderScreen.tsx`, `src/components/order/CartDrawer.tsx`, and `MenuItemCard.tsx`:

1. Align Order Box to the Very Top of the Page (`OrderScreen.tsx`):
   - Adjust the main page layout so the right cart column starts at the exact top edge of the screen (y: 0), extending to the bottom:
     `h-screen w-full bg-[#0A0B0E] p-3 flex gap-3 overflow-hidden`.
   - Left Menu Section: `flex-1 h-full flex flex-col min-w-0 overflow-y-auto pr-1`.
   - Right Cart Section: `w-full lg:w-[410px] h-full flex-shrink-0 flex flex-col`.

2. Multi-Row Dedicated Customer & PAX Header (`CartDrawer.tsx`):
   - Replace the cramped single customer line with a clean 3-row layout so customer names never truncate:
     `<div className="p-4 rounded-2xl bg-[#13151F] border border-white/15 shadow-xl flex flex-col gap-2.5 flex-shrink-0">
        {/* Row 1: Table & Action Buttons */}
        <div className="flex items-center justify-between pb-2 border-b border-white/10">
          <div className="flex items-center gap-2">
            <span className="text-xl font-black text-white tracking-tight">{table.name}</span>
            <span className="px-2.5 py-0.5 rounded-lg bg-emerald-500/20 text-emerald-300 border border-emerald-500/40 text-[10px] font-black uppercase tracking-wider">
              {order.status || "SENT"}
            </span>
          </div>
          <div className="flex items-center gap-1.5">
            <button type="button" onClick={handleMoveTable} className="px-3 py-1.5 rounded-xl bg-sky-500/15 hover:bg-sky-500/25 border border-sky-500/40 text-sky-300 font-black text-xs uppercase tracking-wider flex items-center gap-1 shadow-sm active:scale-95 transition-all">
              ⇄ Move
            </button>
            <button type="button" onClick={handleClearTable} className="px-3 py-1.5 rounded-xl bg-rose-500/15 hover:bg-rose-500/25 border border-rose-500/40 text-rose-300 font-black text-xs uppercase tracking-wider flex items-center gap-1 shadow-sm active:scale-95 transition-all">
              🗑️ Clear
            </button>
          </div>
        </div>

        {/* Row 2: Customer Name & Outstanding Due (Full Width - Zero Truncation) */}
        {customer ? (
          <div className="flex items-center justify-between gap-2 px-3 py-2 rounded-xl bg-[#181B26] border border-white/10 shadow-inner">
            <span className="text-sm font-black text-amber-300 flex items-center gap-1.5 whitespace-nowrap">
              👤 {customer.name}
            </span>
            <div className="flex items-center gap-2">
              {customer.due > 0 && (
                <span className="px-2.5 py-0.5 rounded-lg bg-rose-500/30 border-2 border-rose-500 text-rose-200 text-xs font-black font-mono shadow-[0_0_10px_rgba(244,63,94,0.4)] whitespace-nowrap">
                  DUE: Rs. {customer.due}
                </span>
              )}
              <button type="button" onClick={handleRemoveCustomer} className="p-1 rounded-lg text-zinc-300 hover:text-white hover:bg-white/10 transition-colors">✕</button>
            </div>
          </div>
        ) : (
          <button type="button" onClick={handleAttachCustomer} className="w-full py-2 rounded-xl bg-amber-500/15 hover:bg-amber-500 hover:text-slate-950 border border-amber-500/40 text-amber-300 text-xs font-black uppercase tracking-wider transition-all flex items-center justify-center gap-1.5 shadow-sm">
            + Attach Customer
          </button>
        )}

        {/* Row 3: Server Name & Pax Counter */}
        <div className="flex items-center justify-between text-xs font-bold text-zinc-200 pt-1">
          <span>Served by {staff.name} • {orderItems.length} items</span>
          <div className="flex items-center gap-1.5 bg-[#181B26] border border-white/15 px-2.5 py-1 rounded-xl">
            <span className="text-[10px] font-black uppercase text-amber-400 mr-1">Pax</span>
            <button type="button" onClick={decrementPax} className="w-5 h-5 rounded-md bg-white/10 hover:bg-white/20 text-white font-bold text-xs flex items-center justify-center">-</button>
            <span className="text-xs font-black text-white font-mono w-3 text-center">{guestsPax || 1}</span>
            <button type="button" onClick={incrementPax} className="w-5 h-5 rounded-md bg-white/10 hover:bg-white/20 text-white font-bold text-xs flex items-center justify-center">+</button>
          </div>
        </div>
      </div>`

3. Minimalist Tap-to-Add Menu Cards (`MenuItemCard.tsx` / `OrderScreen.tsx`):
   - REMOVE the redundant bottom-right circular `+` button.
   - REMOVE the "1 IN CART" text tag.
   - Make the entire card a clean tap-to-add action button:
     `<div
        onClick={handleAddItem}
        className="relative p-4 rounded-2xl bg-[#13151F] hover:bg-[#181B26] border border-white/15 hover:border-amber-400/60 shadow-lg transition-all flex flex-col justify-between min-h-[96px] group cursor-pointer active:scale-[0.98]"
      >
        <div className="flex items-start justify-between gap-2">
          <span className="text-base font-black text-white group-hover:text-amber-200 transition-colors tracking-wide leading-snug">
            {item.name}
          </span>
          {itemQty > 0 && (
            <span className="w-7 h-7 rounded-xl bg-amber-500 text-slate-950 font-black text-xs flex items-center justify-center shadow-lg shadow-amber-500/40 border-2 border-[#13151F] flex-shrink-0 animate-in zoom-in-75 duration-150">
              {itemQty}
            </span>
          )}
        </div>
        <div className="mt-2.5">
          <span className="text-base font-black text-amber-400 font-mono tracking-tight">
            Rs. {item.price}
          </span>
        </div>
      </div>`

4. Retain Cart Items Scroll & Checkout Dock:
   - Keep the scrollable items area (`flex-1 min-h-0 overflow-y-auto py-3 space-y-2.5`) and bottom payment action button intact.

STRICT CONSTRAINTS:
- Do NOT alter any item pricing logic, discount calculations, KOT dispatching, or table status sync.
- Run `npm run build` to confirm zero TypeScript or build errors.