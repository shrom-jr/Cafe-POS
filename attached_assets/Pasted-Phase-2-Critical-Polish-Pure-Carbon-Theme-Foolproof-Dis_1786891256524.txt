Phase 2 Critical Polish: Pure Carbon Theme, Foolproof Discount Selector & Zero-Scroll Payment Checkout

Refactor `src/screens/ReviewOrder.tsx`:

1. Pure Carbon Background & Strict Single-Screen Viewport Fit:
   - Root Container: Replace any navy background with pure `#0A0B0E` carbon:
     `h-screen w-full bg-[#0A0B0E] p-4 flex flex-col justify-between overflow-hidden text-white`.
   - Top Header Bar:
     * Back Button & Table Info: `flex items-center gap-3 mb-3 flex-shrink-0`.
     * Title: `text-xl font-black text-white tracking-tight` ("Review Order & Settle • Table {table.name}").
   - Two-Column Grid:
     `grid grid-cols-1 lg:grid-cols-12 gap-5 flex-1 min-h-0 w-full max-w-7xl mx-auto items-stretch`.

2. Left Column: Scrollable Order Items (`col-span-12 lg:col-span-6`):
   - Container: `bg-[#13151F] border border-white/15 p-5 rounded-3xl shadow-xl flex flex-col h-full min-h-0`.
   - Header Row:
     * Title: `text-sm font-black uppercase tracking-wider text-amber-400 flex items-center justify-between`.
     * Count Badge: `px-3 py-0.5 rounded-full bg-white/10 border border-white/15 text-xs font-black text-white font-mono`.
   - Items Scroll List: `flex-1 min-h-0 overflow-y-auto space-y-2.5 pr-1 mt-3 overscroll-contain`.
   - Item Row Card:
     * Surface: `p-3.5 rounded-2xl bg-[#181B26] border border-white/10 hover:border-white/20 transition-all flex items-center justify-between shadow-sm`.
     * Checkbox: `w-5 h-5 rounded-lg border-2 border-white/30 bg-white/5 checked:bg-amber-500 checked:border-amber-400 cursor-pointer`.
     * Name: `text-sm sm:text-base font-black text-white tracking-wide ml-3 flex-1`.
     * Unit Math: `text-xs font-black font-mono text-zinc-200 mt-0.5 block` ("{qty} × Rs. {price}").
     * Line Total: `text-base font-black text-amber-400 font-mono tracking-tight ml-2`.

3. Right Column: Billing Summary & Foolproof Discount Hub (`col-span-12 lg:col-span-6`):
   - Container: `bg-[#13151F] border border-white/15 p-5 rounded-3xl shadow-xl flex flex-col justify-between h-full min-h-0 gap-3 overflow-hidden`.
   
   A. BILLING SUMMARY & FOOLPROOF DISCOUNT SELECTOR:
      - Subtotal Row: `flex items-center justify-between text-xs font-black uppercase tracking-wider text-zinc-200` and `text-base font-black text-white font-mono` ("Rs. {subtotal}").
      
      - FOOLPROOF DISCOUNT COMPONENT:
        * Mode Switcher (Tab Toggle):
          - Container: `flex items-center gap-2 bg-[#181B26] p-1.5 rounded-2xl border border-white/15 mt-2`.
          - Rupees Mode (Rs.): `flex-1 py-1.5 rounded-xl font-black text-xs uppercase tracking-wider transition-all text-center ${mode === 'flat' ? 'bg-amber-500 text-slate-950 shadow-md' : 'text-zinc-300 hover:text-white'}` ("💵 Rupee Discount (Rs.)").
          - Percentage Mode (%): `flex-1 py-1.5 rounded-xl font-black text-xs uppercase tracking-wider transition-all text-center ${mode === 'percent' ? 'bg-amber-500 text-slate-950 shadow-md' : 'text-zinc-300 hover:text-white'}` ("％ Percentage (%)").
        
        * Dedicated Input with Clear Currency Prefix:
          - If Rupees: `[ Rs. ]` prefix with input box placeholder="Enter exact Rs. amount..."
          - If Percentage: `[ % ]` prefix with input box placeholder="Enter percentage (e.g. 10%)..."
          - Input Styling: `w-full bg-[#181B26] border-2 border-white/20 focus:border-amber-400 text-white font-black rounded-xl px-3 py-2 text-sm font-mono outline-none mt-2`.
        
        * Quick Preset Pills:
          - If Rupees Mode: `[ Rs. 50 ] [ Rs. 100 ] [ Rs. 200 ] [ Clear ]` (Pill: `px-3 py-1 rounded-xl bg-white/10 hover:bg-amber-500 hover:text-black border border-white/15 text-xs font-bold text-zinc-100 transition-all`).
          - If Percentage Mode: `[ 5% ] [ 10% ] [ 15% ] [ Clear ]`.
      
      - Total Discount Output: `flex items-center justify-between text-xs font-black uppercase tracking-wider text-rose-300 mt-1` and `text-sm font-black text-rose-400 font-mono` ("- Rs. {calculatedDiscount}").
      
      - Grand Total Row: `flex items-center justify-between pt-2 border-t border-white/10`:
        * Label: `text-xs font-black uppercase tracking-widest text-amber-400` ("FINAL TOTAL DUE").
        * Value: `text-3xl sm:text-4xl font-black text-amber-400 font-mono tracking-tight` ("Rs. {finalTotal}").
      
      - Print Pre-Bill Action: `w-full py-2.5 rounded-xl bg-white/10 hover:bg-white/20 border border-white/20 text-white font-black text-xs uppercase tracking-wider transition-all flex items-center justify-center gap-2 active:scale-95`.

   B. COMPACT PAYMENT METHOD SELECTION GRID (NO CUTOFF / NO OVERFLOW):
      - Container: `flex flex-col gap-2 mt-1`.
      - Section Header: `text-xs font-black uppercase tracking-widest text-amber-400` ("SELECT PAYMENT METHOD").
      - Grid: `grid grid-cols-2 gap-2.5`:
        * Cash: `p-3 rounded-2xl bg-[#0F1916] border-2 border-emerald-500/40 hover:border-emerald-400 flex items-center gap-2.5 transition-all cursor-pointer shadow-md`.
          - Title: `text-xs font-black text-white`.
          - Subtext: `text-[10px] font-bold text-emerald-300 uppercase tracking-wider`.
        * Credit (Khata): `p-3 rounded-2xl bg-[#181510] border-2 border-amber-500/40 hover:border-amber-400 flex items-center gap-2.5 transition-all cursor-pointer shadow-md`.
          - Title: `text-xs font-black text-white`.
          - Subtext: `text-[10px] font-bold text-amber-300 uppercase tracking-wider truncate` ("{customer?.name || 'Select Customer'}").
        * eSewa: `p-3 rounded-2xl bg-[#0F1916] border-2 border-emerald-500/40 hover:border-emerald-400 flex items-center gap-2.5 transition-all cursor-pointer shadow-md`.
          - Title: `text-xs font-black text-white`.
          - Subtext: `text-[10px] font-bold text-emerald-300 uppercase tracking-wider`.
        * Khalti: `p-3 rounded-2xl bg-[#161224] border-2 border-purple-500/40 hover:border-purple-400 flex items-center gap-2.5 transition-all cursor-pointer shadow-md`.
          - Title: `text-xs font-black text-white`.
          - Subtext: `text-[10px] font-bold text-purple-300 uppercase tracking-wider`.
        * Fonepay: `col-span-2 p-2.5 rounded-2xl bg-[#1A1116] border-2 border-rose-500/40 hover:border-rose-400 flex items-center justify-center gap-2 transition-all cursor-pointer shadow-md`.
          - Title: `text-xs font-black text-rose-300 uppercase tracking-wider` ("🔴 Fonepay QR Payment").

STRICT CONSTRAINTS:
- Do NOT alter any billing calculation math, discount application logic, Khata balance updates, or receipt print handlers.
- Run `npm run build` to confirm zero TypeScript or build errors.