Phase 2 Complete Polish: Customers Portal & Modals Carbon-Theme Redesign + Navbar Brand Fix

Refactor `src/screens/CustomersScreen.tsx` (or `Customers.tsx`), `src/components/ui/TopNav.tsx` (or `AppLayout.tsx`), and all Customer pop-up modals (`CollectModal.tsx`, `LedgerModal.tsx`, `CustomerFormModal.tsx`):

1. Standardize Top Navigation Brand Header (`TopNav.tsx` / `AppLayout.tsx`):
   - In the left section of the navbar, ALWAYS display the restaurant title ({settings?.cafeName || "S Bamboo Cottage & Sekuwa Corner"}).
   - DO NOT dynamically replace the restaurant title with route names like "Customers" or "Tables". The brand logo and name must remain static across all tabs.

2. Upgrade Customers Dashboard & Metric Cards (`CustomersScreen.tsx`):
   - Hero Section:
     * Title: `text-2xl font-black text-white dark:text-white tracking-tight` ("Customers & Ledger").
     * Subtitle: `text-xs font-semibold text-slate-400 dark:text-zinc-400 mt-1` ("Credit balances · repayments · ledger history").
     * Register Button: Replace royal blue with a high-contrast amber button:
       `px-5 py-2.5 rounded-xl bg-amber-500 hover:bg-amber-400 active:bg-amber-600 text-slate-950 font-black text-sm shadow-lg shadow-amber-500/20 transition-all flex items-center gap-2 hover:-translate-y-0.5`.
   - Metric Summary Cards (3-Column Grid):
     * Card 1 (Total Customers): `bg-[#13151F] border border-white/10 p-5 rounded-2xl shadow-lg`:
       - Label: `text-[11px] font-black uppercase tracking-wider text-slate-400`.
       - Value: `text-2xl sm:text-3xl font-black text-white mt-1`.
     * Card 2 (Total Dues Outstanding): `bg-[#13151F] border border-rose-500/30 p-5 rounded-2xl shadow-lg shadow-rose-500/5`:
       - Label: `text-[11px] font-black uppercase tracking-wider text-rose-400`.
       - Value: `text-2xl sm:text-3xl font-black text-rose-400 mt-1`.
     * Card 3 (Total Collected): `bg-[#13151F] border border-emerald-500/30 p-5 rounded-2xl shadow-lg shadow-emerald-500/5`:
       - Label: `text-[11px] font-black uppercase tracking-wider text-emerald-400`.
       - Value: `text-2xl sm:text-3xl font-black text-emerald-400 mt-1`.

3. Redesign Customer Directory Table (`CustomersScreen.tsx`):
   - Table Container: `bg-[#13151F] border border-white/10 rounded-2xl overflow-hidden shadow-xl mt-6`.
   - Header: `bg-white/[0.02] border-b border-white/10 text-[11px] font-black uppercase tracking-wider text-slate-400 py-3.5 px-6`.
   - Rows: `border-b border-white/5 hover:bg-white/[0.02] transition-colors py-4 px-6 items-center`:
     * Avatar: `w-9 h-9 rounded-xl bg-[#1E2235] border border-white/10 flex items-center justify-center text-xs font-black text-white shadow-inner`.
     * Customer Name: `text-sm font-black text-white tracking-wide`.
     * Phone: `text-xs font-bold text-slate-400`.
     * Dues Pill:
       - If Clear: `inline-flex items-center gap-1 px-2.5 py-1 rounded-lg bg-emerald-500/10 border border-emerald-500/25 text-emerald-300 text-xs font-black` ("✓ Clear").
       - If Outstanding Due: `inline-flex items-center gap-1 px-2.5 py-1 rounded-lg bg-rose-500/10 border border-rose-500/25 text-rose-400 text-xs font-black` ("Rs. {due}").
     * Actions Cluster (`flex items-center gap-2`):
       - Collect: `px-3 py-1.5 rounded-lg bg-amber-500/15 hover:bg-amber-500/25 active:scale-95 border border-amber-500/40 text-amber-300 text-xs font-black transition-all flex items-center gap-1.5`.
       - Ledger: `px-3 py-1.5 rounded-lg bg-white/5 hover:bg-white/10 active:scale-95 border border-white/10 text-slate-300 text-xs font-bold transition-all flex items-center gap-1.5`.
       - Edit / Delete: Sleek icon buttons in `p-1.5 rounded-lg text-zinc-400 hover:text-white hover:bg-white/5 transition-all`.

4. Refactor All Customer Modals (`CollectModal`, `LedgerModal`, `CustomerFormModal`):
   - Backdrop: `fixed inset-0 bg-black/80 backdrop-blur-md z-50 flex items-center justify-center p-4`.
   - Modal Container: `w-full max-w-md p-6 rounded-3xl bg-[#12141D] border border-white/15 shadow-2xl shadow-black text-white relative`.
   - Inputs: `w-full bg-[#1A1D2A] border border-white/10 focus:border-amber-400 rounded-xl px-4 py-3 text-sm font-bold text-white placeholder-zinc-500 outline-none transition-all`.
   - Payment Method Buttons (in Collect Modal):
     * Active Tab: `bg-amber-500/20 border-2 border-amber-400 text-amber-300 font-black`.
     * Inactive Tab: `bg-white/5 border border-white/10 text-zinc-400 hover:text-white font-bold`.
   - Action Buttons:
     * Confirm Collection: `w-full py-3.5 rounded-xl bg-emerald-500 hover:bg-emerald-400 active:bg-emerald-600 text-slate-950 font-black text-sm transition-all shadow-lg shadow-emerald-500/20`.
     * Register / Save Customer: `w-full py-3.5 rounded-xl bg-amber-500 hover:bg-amber-400 active:bg-amber-600 text-slate-950 font-black text-sm transition-all shadow-lg shadow-amber-500/20`.

STRICT CONSTRAINTS:
- DO NOT alter the Login page, PIN modal, Table Overview (POS), active order logic, Firebase listeners, or khata calculation balances.
- Run `npm run build` to confirm zero TypeScript or build errors.