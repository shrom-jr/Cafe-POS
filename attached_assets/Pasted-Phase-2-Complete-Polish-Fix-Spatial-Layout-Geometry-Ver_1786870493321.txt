Phase 2 Complete Polish: Fix Spatial Layout Geometry, Vertical Cabin Strip & High-Legibility Occupied Cards

Refactor `src/screens/TableOverview.tsx` and `src/components/tables/TableCard.tsx` to fix all layout distortion, text clipping, and cramped typography:

1. High-Legibility Card Redesign (`TableCard.tsx`):
   - Increase default card height to `min-h-[105px]` with `p-3.5 flex flex-col justify-between box-border rounded-2xl w-full transition-all`:
     * Header Row: Table Name on left in prominent `text-xl font-black text-white dark:text-white` (or `text-slate-950` in light mode); Status pill on right: `text-[11px] font-black tracking-wider uppercase px-2.5 py-1 rounded-lg bg-amber-500/20 text-amber-300 border border-amber-500/40`.
     * Middle Row (Guests & Customer): `flex items-center gap-2.5 py-1`:
       - Guest Count: `text-sm font-bold text-slate-200 dark:text-slate-200 flex items-center gap-1` (e.g., `👤 1 Guest`).
       - Customer Badge: `bg-amber-500/20 border border-amber-500/40 text-amber-200 font-bold px-2.5 py-0.5 rounded-md text-xs truncate max-w-[130px]`.
     * Footer Row (Timer & Items): `flex items-center justify-between text-xs font-bold text-slate-300 dark:text-slate-300 pt-2 border-t border-white/10 dark:border-white/10`:
       - Timer: `⏱️ {timer}` in `text-amber-400 font-bold text-xs`.
       - Items: `{itemCount} {itemCount === 1 ? 'Item' : 'Items'}` in `text-slate-200 dark:text-slate-200 text-xs`.
   - Free Table Cards:
     * `min-h-[105px] h-full w-full flex items-center justify-center p-3 rounded-2xl`:
     * Centered Table Name in `text-2xl font-black tracking-wide`.
     * Light Mode: `bg-slate-100 hover:bg-slate-200 border-[1.5px] border-slate-900 text-slate-950 shadow-sm`.
     * Dark Mode: `bg-[#13151F] border border-white/10 text-white shadow-sm`.

2. Correct Spatial Layout Geometry (`TableOverview.tsx`):
   - Wrap the entire floor inside `max-w-7xl mx-auto px-4 py-4 w-full`:
   
   A. FIRST FLOOR (HUTS & HALL) - 3 Columns (`grid grid-cols-12 gap-5 items-stretch`):
      - Col 1 (cols 1-4, Hut 3): Stack of 3 cards (`H3-B`, `H3-A`, `T-1`), each `min-h-[100px]` with `gap-3`.
      - Col 2 (cols 5-8, Hut 2): Stack of 3 cards (`H2-B`, `H2-A`, `T-2`), each `min-h-[100px]` with `gap-3`.
      - Col 3 (cols 9-12, Hut 1): Single `H1` hut card spanning the height of the column (`h-full min-h-[320px]`) with centered interior content when free, or spacious 3-row layout when occupied.

   B. GROUND FLOOR & COURTYARD - 3 Columns (`grid grid-cols-12 gap-5 items-start`):
      - Left Column (Sofa & Lounge, cols 1-4):
        * Top: Wide "Sofa" group card (`min-h-[100px]`).
        * Middle: 2x2 grid for `L4`, `L3` (top) and `L2`, `L1` (bottom) (each `min-h-[100px]`, `gap-3`).
        * Bottom: `[ 📺 TV Area ]` landmark pill (`py-2 rounded-xl text-xs font-bold text-center`).
      - Middle Column (Bar & Courtyard, cols 5-8):
        * Top: 2-column grid for `Bar 1` and `Bar 2` side-by-side (`min-h-[100px]`, `gap-3`).
        * Middle: Landmark badge `[ 🅿️ Parking Area ]` (`py-4 rounded-xl text-xs font-bold text-center`).
        * Bottom: Landmark badge `[ ⛩️ Main Gate ]` (`py-4 rounded-xl text-xs font-bold text-center`).
      - Right Column (Private Cabins, cols 9-12):
        * Back Quad: 2x2 grid for `R3`, `R1` (top row) and `R4`, `R2` (bottom row) (each `min-h-[100px]`, `gap-3`).
        * Front Strip (CRITICAL FIX): Stack `R5`, `R6`, `R7` VERTICALLY (one below the other, each `min-h-[90px]`, `gap-3`) inside a dedicated front strip container. DO NOT place them in a cramped 3-column horizontal row.

STRICT CONSTRAINTS:
- Retain all Firebase Realtime Database listeners, active order state calculations, drawer click handlers, and theme styling tokens.
- Run `npm run build` to confirm zero TypeScript or build errors.