Phase 2 Complete Redesign: Multi-Million Dollar Luxury POS Terminal Login Experience

Refactor `src/screens/PinLoginScreen.tsx` (and all associated PIN/staff modal components) into a high-end, luxury hospitality command center:

1. Ambient Canvas & Visual Depth (`PinLoginScreen.tsx`):
   - Background Canvas:
     * Dark Mode: `bg-[#07080B] min-h-screen relative flex items-center justify-center p-4 overflow-hidden`. Add a subtle ambient backlight glow behind the center card:
       `<div className="absolute top-1/4 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[600px] h-[350px] bg-amber-500/10 rounded-full blur-[120px] pointer-events-none" />`
     * Light Mode: `bg-slate-100 min-h-screen relative flex items-center justify-center p-4`.
   - Floating Theme Toggle: Place a sleek theme switcher in the top right (`absolute top-6 right-6 z-30`).

2. Elevated Glassmorphic Command Hub Card (`PinLoginScreen.tsx`):
   - Wrap the entire login experience inside a single, high-end elevated glass container:
     `relative z-10 w-full max-w-4xl mx-auto rounded-3xl p-8 sm:p-12 backdrop-blur-2xl bg-[#10121A]/85 dark:bg-[#10121A]/90 border border-white/10 dark:border-white/10 shadow-2xl shadow-black/80 flex flex-col items-center`.

3. Brand Hero Header & Hardcoded Fallback:
   - Logo: `w-20 h-20 rounded-2xl border-2 border-amber-500/50 bg-[#161822] p-2.5 shadow-xl shadow-amber-500/15 mb-4 object-contain`.
   - Restaurant Name (CRITICAL FIX): NEVER render an empty string. Always use fallback:
     `<h1 className="text-2xl sm:text-3xl lg:text-4xl font-extrabold tracking-tight text-white dark:text-white text-center">
        {settings?.cafeName || "S Bamboo Cottage & Sekuwa Corner"}
      </h1>`
   - Subtitle: `<p className="text-xs font-black tracking-[0.25em] text-amber-400 dark:text-amber-400 uppercase mt-1.5 text-center">STAFF ACCESS • POS TERMINAL</p>`
   - Instruction Badge: `<div className="mt-4 px-4 py-1.5 rounded-full bg-white/5 border border-white/10 text-xs font-semibold text-zinc-300">Tap Your Profile to Sign In</div>`

4. Luxury Staff Profile Cards (Balanced 3 + 2 Grid):
   - Container: `flex flex-wrap justify-center gap-4 w-full mt-8 max-w-3xl mx-auto`.
   - Card (`w-[230px] sm:w-[240px] p-4 rounded-2xl flex items-center gap-3.5 cursor-pointer transition-all duration-200 bg-[#161824] hover:bg-[#1C2030] border border-white/10 hover:border-amber-500/60 shadow-lg hover:shadow-amber-500/10 hover:-translate-y-1 group`):
     * Avatar: `w-12 h-12 rounded-xl flex-shrink-0 bg-[#202436] border border-white/15 flex items-center justify-center text-sm font-black text-white group-hover:scale-105 transition-transform shadow-inner`.
     * Staff Info:
       - Name: `text-sm font-black text-white truncate`.
       - Role Tag: `mt-1 self-start px-2.5 py-0.5 rounded-md text-[10px] font-black uppercase tracking-wider border`:
         * Admin: `bg-purple-500/15 text-purple-300 border-purple-500/30`
         * Cashier: `bg-sky-500/15 text-sky-300 border-sky-500/30`
         * Waiter: `bg-emerald-500/15 text-emerald-300 border-emerald-500/30`
         * Kitchen: `bg-amber-500/15 text-amber-300 border-amber-500/30`

5. Tactile Glass PIN Keypad Modal:
   - Container: `max-w-xs w-full p-6 rounded-3xl bg-[#12141D] border border-white/15 shadow-2xl shadow-black flex flex-col items-center`.
   - Header: Staff avatar initial, name, and role badge on the left; close `✕` on the right.
   - Indicator: 4 animated dots with glowing amber fill (`bg-amber-500 border-amber-500 shadow-md shadow-amber-500/50`) when typed.
   - Keypad Grid: Circular glass buttons (`h-14 w-14 rounded-full text-xl font-bold bg-white/5 hover:bg-white/15 active:scale-95 border border-white/10 active:border-amber-500/50 text-white flex items-center justify-center transition-all`).
   - Bottom Row: `⌫` (backspace), `0`, `✕` (cancel).

STRICT CONSTRAINTS:
- Do NOT alter any staff user data, PIN validation logic, role permission routing, or Firebase Auth tokens.
- Run `npm run build` to confirm zero TypeScript or build errors.