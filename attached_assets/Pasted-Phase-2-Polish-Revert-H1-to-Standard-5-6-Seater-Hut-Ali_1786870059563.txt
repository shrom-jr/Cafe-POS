Phase 2 Polish: Revert H1 to Standard 5-6 Seater Hut & Align First Floor Card Proportions

Refactor `src/screens/TableOverview.tsx` and `src/components/tables/TableCard.tsx`:

1. H1 Name & Content Fix (`TableOverview.tsx`, `TableCard.tsx`, `venueSeed.ts`):
   - Change the display label and database name from "H1 - GROUP HALL" back to simply "H1".
   - Treat H1 as a standard 5-6 guest dining hut.
   - When H1 is occupied, format its interior identically to other occupied cards:
     * Header: "H1" (`text-lg font-black`) + `• OCCUPIED` badge.
     * Middle: `👤 {guests} Guests` + customer pill tag.
     * Footer: `⏱️ {timer}` + `{itemCount} Items`.
   - Remove excessive empty height from H1: give H1 a clean, balanced card height (`min-h-[140px]` or matching height with centered contents, not an empty tall box).

2. First Floor Column Alignment (`TableOverview.tsx`):
   - Structure First Floor as a clean 3-column grid (`grid grid-cols-12 gap-4 items-start`):
     * Col 1 (cols 1-4, Hut 3): Stacked H3-B (`h-[92px]`), H3-A (`h-[92px]`), T-1 (`h-[92px]`).
     * Col 2 (cols 5-8, Hut 2): Stacked H2-B (`h-[92px]`), H2-A (`h-[92px]`), T-2 (`h-[92px]`).
     * Col 3 (cols 9-12, Hut 1): Clean H1 dining card spanning the column with balanced vertical centering and no dead space.

3. Standardize Occupied Card Internal Layout across all 21 tables (`TableCard.tsx`):
   - Every occupied card must strictly follow a 3-row layout with `h-full w-full flex flex-col justify-between p-3 box-border`:
     * Row 1 (Header): Table Name on left (`text-lg font-black text-white dark:text-white`), `• OCCUPIED` badge on right (`text-[10px] font-bold px-2 py-0.5 rounded-md bg-amber-500/20 text-amber-400 border border-amber-500/40`).
     * Row 2 (Middle Info): `flex items-center gap-2 text-xs font-semibold text-slate-300 dark:text-slate-300`:
       - `👤 {guests} Guests`
       - Customer pill: `bg-amber-500/15 border border-amber-500/30 text-amber-300 px-2 py-0.5 rounded text-[11px] font-bold truncate max-w-[120px]`
     * Row 3 (Footer Divider): `flex items-center justify-between text-xs font-bold text-slate-300 dark:text-slate-300 pt-1.5 border-t border-white/10`:
       - `⏱️ {timer}` (text-amber-400)
       - `{itemCount} {itemCount === 1 ? 'Item' : 'Items'}`

STRICT CONSTRAINTS:
- Do not alter Firebase listeners, active order state calculations, drawer click handlers, or theme tokens.
- Run `npm run build` to confirm zero TypeScript or build errors.