Phase 2 Polish: Pure Black Glass PIN Modal, Dynamic Venue Title, Logo Styling & Glare Removal

Refactor `src/screens/PinLoginScreen.tsx` and all associated login/PIN modal components:

1. Clean Canvas & Glare Removal (`PinLoginScreen.tsx`):
   - REMOVE the ambient orange/amber blurred glow element completely (`bg-amber-500/10 blur-[120px]`).
   - Use a clean canvas:
     * Dark Mode: `bg-[#0A0B0E] text-white min-h-screen relative flex items-center justify-center p-4`.
     * Light Mode: `bg-slate-100 text-slate-950 min-h-screen relative flex items-center justify-center p-4`.

2. Dynamic White-Label Brand Header (`PinLoginScreen.tsx`):
   - Dynamic Title: Pull dynamically from settings with a generic POS fallback:
     `<h1 className="text-2xl sm:text-3xl font-black tracking-tight text-white dark:text-white text-center">
        {settings?.cafeName || settings?.restaurantName || "Point of Sale"}
      </h1>`
   - Subtitle: `<p className="text-xs font-black tracking-[0.2em] text-amber-400 uppercase mt-1.5 text-center">STAFF ACCESS • POS TERMINAL</p>`
   - Clean Logo Container:
     * Remove the muddy amber double border.
     * Render the logo inside a clean rounded container: `w-20 h-20 rounded-2xl bg-white/5 border border-white/10 p-2 shadow-xl mb-4 mx-auto flex items-center justify-center overflow-hidden`.
     * Image styling: `w-full h-full object-contain rounded-xl`.

3. Pure Black Glass PIN Modal (`PinLoginScreen.tsx` / `PinModal.tsx`):
   - Modal Container:
     `max-w-xs w-full p-6 rounded-3xl bg-black/95 backdrop-blur-2xl border border-white/20 shadow-2xl shadow-black flex flex-col items-center z-50`.
   - Header Section:
     * Staff Initial Avatar + Name in `text-base font-black text-white` + Role Badge.
     * High-contrast close button (`✕`) in `text-zinc-400 hover:text-white transition-colors`.
   - PIN Indicator Dots:
     * 4 circles with high contrast: `h-3.5 w-3.5 rounded-full border-2 border-white/30 transition-all`.
     * Active filled state: `bg-amber-400 border-amber-400 shadow-md shadow-amber-400/60`.
   - High-Contrast Keypad Grid:
     * Circular buttons: `h-14 w-14 rounded-full text-2xl font-bold bg-white/10 hover:bg-white/20 active:scale-95 border border-white/15 active:border-amber-400 text-white flex items-center justify-center transition-all shadow-sm`.
     * Bottom Row: Backspace (`⌫`), `0`, and Cancel (`✕`) buttons with clean vector/text contrast.
   - High-Contrast "Forgot PIN?" Link:
     * Style as a crisp link: `<button className="mt-4 text-xs font-bold text-amber-400 hover:text-amber-300 hover:underline transition-colors tracking-wide">Forgot PIN?</button>`.

4. Staff Profile Badges (Preserve Balanced Grid):
   - Maintain the balanced 3+2 centered grid (`flex flex-wrap justify-center gap-4 w-full mt-8 max-w-3xl mx-auto`).
   - Cards: `bg-[#13151F] border border-white/10 hover:border-amber-500/60 hover:bg-[#181B26] p-4 rounded-2xl flex items-center gap-3.5 cursor-pointer transition-all hover:-translate-y-1`.

STRICT CONSTRAINTS:
- Do NOT alter any staff user data, PIN validation logic, role permission routing, or Firebase Auth tokens.
- Run `npm run build` to confirm zero TypeScript or build errors.