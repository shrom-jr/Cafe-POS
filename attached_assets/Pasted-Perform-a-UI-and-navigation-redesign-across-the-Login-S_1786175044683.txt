Perform a UI and navigation redesign across the Login Screen, PIN Modal, Top Navigation Header, and Tables Floor View to give the POS a modern, commercial-grade appearance. Do NOT alter any backend state, table schemas, permissions logic, or order calculations.

### 1. Login & PIN Pad UI Overhaul
1. Login Screen:
   - Convert to a split layout:
     * Left Panel: Restaurant branding container with Cafe name, subtle dark gradient backdrop, live digital clock, and current date.
     * Right Panel: Staff profile cards grid using frosted glass styling, smooth hover-lift animations, vibrant avatars, and role-colored badges (Admin: Gold, Cashier: Emerald, Kitchen: Orange, Waiter: Blue).
2. PIN Modal:
   - Replace the flat overlay with a smooth frosted glass backdrop (backdrop-blur).
   - Style the numeric keypad with tactile circular buttons, touch-active press effects, glowing PIN indicator dots, and clear Cancel/Forgot PIN actions.

### 2. Header & Command Navigation Drawer
1. Top Header Cleanup:
   - Remove the centered raw text navigation tabs (Tables, History, Kitchen Portal, Bar Portal, Admin) from the header.
   - Left Side: Add a sleek Command Drawer trigger button (Hamburger ☰ icon) alongside the restaurant brand name and a live connection indicator (🟢 Live).
   - Right Side: Retain active table counters (Available / Active), live clock, staff profile badge, and "Switch User" button.
2. Slide-Out Navigation Drawer:
   - Implement a dark slide-out menu drawer triggered by the ☰ menu icon.
   - Filter menu items using the active logged-in staff user's existing feature permissions:
     * 📱 Tables & POS (requires POS & Tables permission)
     * 📜 Order History (requires POS & Tables permission)
     * 🍳 Kitchen Portal (requires Kitchen Portal permission)
     * 🍺 Bar Portal (requires Bar Portal permission)
     * ⚙️ Admin Panel (requires Admin Panel permission)
   - Style each drawer item with modern vector icons, active highlights, and smooth open/close transitions.

### 3. Multi-Area Table Floor View Polish
1. Filter Pills & Area Headers:
   - Refine the area filter pills (All, Ground Floor, Cabins, First Floor) with active fill highlights and count chips.
   - Keep area section dividers clean with subtle accent icons and live status counters.
2. Table Card Visuals (Preserve All Existing Metrics):
   - Upgrade table card container styling using modern rounded corners, glassmorphic borders, and status lighting (Glowing Green border for Available, Glowing Amber border for Active).
   - Maintain and cleanly format all existing dynamic table data: Table Name, Status Badge, Pax Count, Item Count, and Running Timer (e.g., "1 item · 0:03").

### 4. Safety & Scope Rules
- Do NOT modify table state hooks, Firebase sync logic, order stores, or staff permissions checks.
- Ensure all existing click handlers for selecting tables, opening KOT modals, and switching users remain fully functional.