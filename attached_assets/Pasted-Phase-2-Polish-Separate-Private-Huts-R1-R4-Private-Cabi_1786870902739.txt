Phase 2 Polish: Separate "Private Huts" (R1-R4) & "Private Cabins" (R5-R7) with Matching 2-Column Grid Alignment

Refactor `src/screens/TableOverview.tsx` and `src/utils/venueSeed.ts` to update the right column layout and area categorization:

1. Separate Right Column into Two Distinct Groups (`TableOverview.tsx`):
   - Replace the single combined "PRIVATE CABINS" right column with two clearly labeled sub-sections:
   
   A. PRIVATE HUTS (Section Header: `| PRIVATE HUTS`):
      - 2-Column Grid (`grid grid-cols-2 gap-3`):
        * Top Row: R3 (left), R1 (right)
        * Bottom Row: R4 (left), R2 (right)
      - Each card maintains standard height `min-h-[100px]` with full occupied/free styling.

   B. PRIVATE CABINS (Section Header: `| PRIVATE CABINS`):
      - 2-Column Grid (`grid grid-cols-2 gap-3` matching R1-R4's exact width and height):
        * Top Row: R5 (left), R6 (right)
        * Bottom Row: R7 (left, taking a single grid column width—DO NOT stretch across both columns)
      - Ensure R5, R6, R7 cards have the exact same width, height (`min-h-[100px]`), padding, and card proportions as R1–R4.

2. Update Area Registry & Filter Pills (`venueSeed.ts` & `TableOverview.tsx`):
   - Update canonical areas to reflect the distinct separation:
     * `First Floor (Huts & Hall)` (7 tables: H3-B, H3-A, T-1, H2-B, H2-A, T-2, H1)
     * `Sofa & Lounge` (5 tables: Sofa, L4, L3, L2, L1)
     * `Bar Counter` (2 tables: Bar 1, Bar 2)
     * `Private Huts` (4 tables: R1, R2, R3, R4)
     * `Private Cabins` (3 tables: R5, R6, R7)
   - Ensure the top filter bar displays the dedicated filter pills: `[ All (21) ]` `[ First Floor (7) ]` `[ Sofa & Lounge (5) ]` `[ Bar Counter (2) ]` `[ Private Huts (4) ]` `[ Private Cabins (3) ]`.

3. Preserve Card Styling & Functional Integrity:
   - Retain the high-contrast occupied card layout (`min-h-[100px]`, bold table names, guest counts, customer tags, live timers, and item counts).
   - Retain light/dark theme contrast tokens and all POS drawer navigation/active order listeners.

STRICT CONSTRAINTS:
- Run `npm run build` to confirm zero TypeScript or build errors.