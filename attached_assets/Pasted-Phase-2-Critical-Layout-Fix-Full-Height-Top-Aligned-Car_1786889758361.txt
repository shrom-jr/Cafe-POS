Phase 2 Critical Layout Fix: Full-Height Top-Aligned Cart Drawer & Slim Bottom Total Dock

Refactor `src/screens/OrderScreen.tsx` and `src/components/order/CartDrawer.tsx`:

1. Top-Level Page Layout Fix (`OrderScreen.tsx`):
   - Restructure the top-level page grid so the right cart column starts at the very top edge (y: 0), parallel to the header bar, while the left column contains the header, search, categories, and menu grid:
     `<div className="h-screen w-full bg-[#0A0B0E] p-3 flex gap-3 overflow-hidden">
        {/* Left Column (Menu System) */}
        <div className="flex-1 h-full flex flex-col min-w-0 overflow-y-auto pr-1 gap-3">
          {/* Top Bar: Back button, Status, Theme toggle */}
          <div className="flex items-center justify-between pb-1 flex-shrink-0">
            <div className="flex items-center gap-3">
              <button type="button" onClick={handleBack} className="px-3 py-1.5 rounded-xl bg-white/10 hover:bg-white/15 text-white font-black text-xs flex items-center gap-1.5 transition-all">
                ‹ Back
              </button>
              <div className="flex items-center gap-2">
                <span className="text-xs font-black uppercase tracking-wider text-zinc-300">ORDER STATUS:</span>
                <span className="px-2.5 py-0.5 rounded-md bg-emerald-500/20 text-emerald-300 border border-emerald-500/40 text-[10px] font-black uppercase tracking-wider">
                  {order.status || "SENT"}
                </span>
              </div>
            </div>
            <ThemeToggle />
          </div>

          {/* Search Bar */}
          <input ... />

          {/* Categories & Subcategories */}
          <CategoriesBar ... />

          {/* Menu Items Grid */}
          <MenuItemsGrid ... />
        </div>

        {/* Right Column (Top-Aligned Full-Height Cart Drawer) */}
        <div className="w-full lg:w-[410px] h-full flex-shrink-0 flex flex-col">
          <CartDrawer ... />
        </div>
      </div>`

2. Standalone Full-Height Cart Shell (`CartDrawer.tsx`):
   - Container Shell:
     `w-full h-full rounded-3xl bg-[#13151F] border border-white/15 shadow-2xl flex flex-col justify-between overflow-hidden p-3.5 gap-2.5`.

3. Compact Top Metadata Header (`CartDrawer.tsx`):
   - Header Container: `p-3 rounded-2xl bg-[#181B26] border border-white/10 shadow-sm flex flex-col gap-2 flex-shrink-0`.
   - Row 1 (Table Title & Actions):
     * Table Name: `text-lg font-black text-white tracking-tight`.
     * Status Pill: `px-2 py-0.5 rounded-md bg-emerald-500/20 text-emerald-300 border border-emerald-500/40 text-[10px] font-black uppercase`.
     * Move & Clear: `px-2.5 py-1 rounded-lg text-xs font-black uppercase`.
   - Row 2 (Customer & Due Tag):
     * Container: `flex items-center justify-between gap-2 pt-1.5 border-t border-white/10`.
     * Customer Name: `text-xs font-black text-amber-300 truncate`.
     * Due Badge: `px-2 py-0.5 rounded-md bg-rose-500/30 border border-rose-500 text-rose-200 text-[11px] font-black font-mono shadow-[0_0_8px_rgba(244,63,94,0.3)] whitespace-nowrap`.
   - Row 3 (Server & Pax Stepper):
     * Text: `text-[11px] font-bold text-zinc-300`.
     * Pax Stepper: Compact `px-2 py-0.5 rounded-lg bg-white/5 border border-white/10 text-xs text-white`.

4. Maximized Scrollable Cart Items Area:
   - Container: `flex-1 min-h-0 overflow-y-auto space-y-2 pr-1 overscroll-contain`.
   - Cart Item Cards:
     * Surface: `p-3 rounded-2xl bg-[#161824] border border-white/15 shadow-sm flex flex-col gap-1.5`.
     * Title: `text-xs sm:text-sm font-black text-white tracking-wide`.
     * Price: `text-xs font-black text-amber-400 font-mono`.

5. Slim Compact Bottom Total & Payment Dock:
   - Dock Container: `p-3 rounded-2xl bg-[#181B26] border border-white/15 flex flex-col gap-2 shadow-xl flex-shrink-0 mt-auto`.
   - Total Row: `flex items-center justify-between`:
     * Label: `text-[11px] font-black uppercase tracking-widest text-zinc-300` ("TOTAL").
     * Value: `text-2xl font-black text-white font-mono tracking-tight` ("Rs. {total}").
   - Status Alert: `<p className="text-[10px] font-black uppercase tracking-wider text-emerald-400 text-center">✓ ORDER ACTIVE & SENT TO KITCHEN</p>`.
   - Primary Submit Button:
     `w-full py-3 rounded-xl bg-amber-500 hover:bg-amber-400 active:bg-amber-600 text-slate-950 font-black text-xs sm:text-sm uppercase tracking-wider shadow-lg shadow-amber-500/25 transition-all active:scale-[0.98] flex items-center justify-center gap-1.5`.

STRICT CONSTRAINTS:
- Do NOT alter any item pricing logic, tax calculations, KOT print dispatching, or table status sync.
- Run `npm run build` to confirm zero TypeScript or build errors.