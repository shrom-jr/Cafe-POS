Phase 2 Polish: Restore Full-Width Edge-to-Edge Layout in Bar Portal with Balanced Viewport Fit

Refactor `src/screens/BarPortal.tsx`:

1. Restore Full-Width Layout (NO Narrow Boxed Margins):
   - REMOVE any `max-w-7xl` or narrow container restrictions.
   - Use the full screen canvas width matching the Tables and Customers portals:
     `w-full px-6 py-4 flex flex-col gap-4`.

2. Top Metric Cards (Full Width 3-Column Grid):
   - Container: `grid grid-cols-1 md:grid-cols-3 gap-5 w-full`.
   - Cards (Today's Entries, Total Spend, Items Updated):
     * Container: `p-5 rounded-2xl bg-[#13151F] border border-white/15 shadow-xl flex flex-col justify-center`.
     * Labels: `text-xs font-black uppercase tracking-wider text-amber-400` (Total Spend: `text-emerald-400`).
     * Values: `text-3xl font-black text-white mt-1` (Total Spend: `text-3xl font-black text-emerald-400`).

3. Two-Column Workspace (Full Width & Balanced Vertical Height):
   - Container: `grid grid-cols-12 gap-5 w-full items-stretch`.
   
   A. LEFT COLUMN - LOG BAR ENTRY (`col-span-12 lg:col-span-4`):
      - Container: `bg-[#13151F] border border-white/15 p-5 rounded-3xl shadow-xl flex flex-col gap-3.5`.
      - Header: Title in `text-base font-black text-white` with `+ New Item` link in `text-xs font-black text-amber-400 hover:text-amber-300 ml-auto`.
      - Field Labels ("PRODUCT", "ENTRY TYPE", "QUANTITY", "TOTAL COST", "SUPPLIER"):
        * `text-xs font-black uppercase tracking-wider text-amber-400 mb-1 block`.
      - Inputs: `w-full bg-[#181B26] border-2 border-white/20 focus:border-amber-400 text-white font-bold rounded-xl px-4 py-2.5 text-sm placeholder:text-zinc-500 outline-none shadow-inner`.
      - Entry Type Buttons: `py-2.5 px-3 rounded-xl text-xs font-black uppercase`.
      - Submit Button: `w-full py-3.5 rounded-xl bg-amber-500 hover:bg-amber-400 active:bg-amber-600 text-slate-950 font-black text-sm uppercase tracking-wider shadow-lg shadow-amber-500/25 transition-all active:scale-[0.98] mt-1`.

   B. RIGHT COLUMN - TODAY'S RESTOCKS (`col-span-12 lg:col-span-8`):
      - Container: `bg-[#13151F] border border-white/15 p-6 rounded-3xl shadow-xl flex flex-col min-h-[420px]`.
      - Header: `text-base font-black text-white flex items-center justify-between` with date badge on the right.
      - Empty State Box: `flex-1 flex flex-col items-center justify-center text-center p-8 border-2 border-dashed border-white/10 rounded-2xl bg-white/[0.02] mt-4`.

4. Preserve Custom Downward Carbon Dropdown:
   - Ensure the dropdown remains the custom carbon `#12141D` component that opens downwards over the form with amber hover highlights and bold white text.

STRICT CONSTRAINTS:
- Do NOT alter any inventory calculations, Firebase sync listeners, or other portals.
- Run `npm run build` to confirm zero TypeScript or build errors.