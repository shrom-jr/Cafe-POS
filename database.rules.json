{
  "deploymentBlocked": "INDEX TEMPLATE ONLY — Firebase Authentication is not configured for this project. This extra top-level field intentionally makes the file invalid for Firebase rules deployment. Remove it and replace the public root rules with authenticated, path-specific rules only after a Firebase Auth rollout.",
  "rules": {
    ".read": true,
    ".write": true,
    "orders": {
      ".indexOn": ["status", "tableId", "createdAt"]
    },
    "payments": {
      ".indexOn": ["createdAt", "customerId", "method"]
    },
    "kitchenPurchases": {
      ".indexOn": ["createdAt", "supplierId"]
    },
    "invMappings": {
      ".indexOn": ["menuItemId", "productId", "inventoryType"]
    },
    "customers": {
      ".indexOn": ["phone", "lastVisit", "currentDue"]
    },
    "tables": {
      ".indexOn": ["status", "area", "orderId"]
    }
  }
}